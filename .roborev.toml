review_context_count = 2   # Recent reviews to include as context
review_guidelines = """
Always run python and checks via pixi and prek:
- `pixi run -e tests-cpu ty`
- `pixi run -e tests-cpu tests`
- `prek run --all-files`

All docstrings use mystmd syntax

This is Python 3.14 only. `except ValueError, TypeError` is perfectly valid syntax.

We are using ty as the type checker, so `ty: ignore` is the preferred syntax.

Read CLAUDE.md for further instructions.
"""
