# Next evidence — two bounded tasks, no new production repair

**G1–G3 and prior U1 are accepted on the supplied source-linked CPU evidence. Do not rerun them merely because older notes call them pending.** The remaining obligation is **U2/G4**, not an uncovered numerical defect or a new economic-grid oracle project. The author decision below is not a reviewer-issued resource waiver or job authorization.

## 1. Root-owned G4 work order and claim disposition

**Owner:** PyLCM root integration/review maintainer. **Files:** retain `DECISION.md`, `acceptance/continuous-assets-eightway.json`, and `evidence/candidate-set-assessment.json`; prepare an owner-local `acceptance/continuous-assets-eightway-g4-WORKORDER.md` and its source/harness manifest. No production source edit is required by this review.

**Decisive gate:** choose explicitly between keeping the currently accepted CPU-tested/represented-admission scope with G4 open, or authorizing the narrowly scoped resource experiment below. A clean/closed **resource/hardware** verdict requires the measurements and disposition, not merely renaming the open gate. Preserve the balanced contract digest and the finite-candidate bound; do not manufacture a continuum epsilon or reopen an unrelated refinement matrix.

The work order must pin the intended production revision (this review's head is `aaefa6ff86441906f50acbbf001dae64439ad2ba`), the baseline `620813df93b6a50db4383ffc43690dc70e4ae8c3`, actual harness/test bytes, native payload, source/environment lock hashes and any frozen-environment exception. A later source change needs a delta identity record, not a transplanted clean label. Identify an exact main comparison pin only where the resource contract requires that comparison; no main result is supplied here.

Freeze the existing 3×3×24 economic witness, actions, parameters, source-pinned tolerances, seeds, diagnostics and retention. Freeze one already-supported discrete control for a genuine parent→head comparison. The old baseline refuses the **new continuous sharded request**; it has no eight-way continuous timing. The new topology comparison is **same-head one device versus same-head eight devices**, not a manufactured baseline regression.

**Resources:** documentation/source review only for this task; no numerical solve, install, production allocation or scheduler mutation. Existing 3-A40 ACA ownership and its sealed source remain untouched.

**Stop:** no owner-approved work order, unavailable exact source/native identity, missing eight-device hardware, or a required policy choice left unresolved means G4 stays open. Do not substitute older ACA data, outer C0/C1 ratios, a smaller actual mesh or a skipped CPU-only test.

## 2. Conditional owner-run G4 receipt, only after task 1 authorizes it

**Owner:** the root-designated native hardware/measurement owner, not the reviewer. **Files:** the approved work order, a source-sealed backend-appropriate driver under `acceptance/`, and the single result **`acceptance/continuous-assets-eightway-g4.json`**, with raw command/exit/JUnit or equivalent assertion logs and measurement data in the same evidence directory. This is a future artifact specification, not an executable harness delivered or run by this reply.

**Decisive gate:** preserve the already established numerical/ownership meaning on the actual hardware and report all required resource quantities without mixing workloads. The shipped native test modules explicitly require CPU; invoking them unchanged on GPU would skip and cannot establish this gate. Any owner-prepared GPU driver must retain the actual shard-index, source-survival, global-read, numerical/decision/RNG and refusal assertions. Removing a topology check or accepting skipped assertions is not a port of the witness.

The receipt must contain the following compact records:

| Record | Required content |
|---|---|
| Identity and topology | Source/harness/native/library hashes, actual Python/JAX/JAXLIB/backend/dtype, cache and allocator settings, device identifiers/UUIDs, actual global indices and local shapes on all eight selected devices. |
| Comparisons | Same-head continuous one→eight topology experiment; separate same-harness baseline→head supported discrete control. Keep any required main→head series separately pinned and labelled. No “baseline eight-way continuous” measurement. |
| Completed calls | One fresh cold and at least three synchronized warm public calls per admitted arm/precision, with unchanged retention, inputs, seeds and logging. Separate solve, supplied-solution simulation and automatic solve+simulation whenever measured; do not silently combine their denominators. |
| Compilation | Direct lowering/backend/orchestration instrumentation, cold public wall time separately, actual warm backend request counts. Cold-minus-warm is not compilation time; repeated continuous-route backend requests must be zero. |
| Memory and admission | Per-selected-device allocator **live high-water**, host peak with instrument/scope, represented compiler P and retained/destination/scratch H/R/T, and actual live-root/admission/refusal records. Pool reservations, process GPU totals, RSS and compiler reservations are not substitutes for device allocator peaks. |
| Correctness and disposition | Source-pinned value tolerance, exact separated choices, masks/RNG/real rows, source lifetime and actual shard coverage; precision-separated failures/refusals. Record raw samples, variation and any explicit maintainer acceptance of a 5% warm or 10% compile/peak-memory review-trigger breach. |

Use fresh same-SHA controls where affordable to assess variation. Lesser replication or an unavailable instrument must remain explicit; it does not justify a causal claim or a silently passed profile field. The retained three-warm outer measurements and their zero backend counts remain a different experiment. Do not claim the CPU transport threshold is a physical allocator ceiling.

**Resources:** one explicitly approved tiny witness packet, within the existing owner rules: bounded Marvin CPU prerequisites, no more than 180 seconds per reviewer-style CPU witness and 600 seconds of reviewer CPU execution, and an owner-approved GPU allocation under the existing `mlgpu_short` eight-hour ceiling with watchdog/cleanup margin. This is a ceiling, not an expected runtime or permission to launch. Avoid multiplying every width, seed, topology and solver into a new full matrix. No ACA production experiment or modification of the standing 3-A40 allocation is part of this task.

**Stop:** any semantic, lifetime, pre-dispatch-admission or source-identity failure; unexpected skips; native fatal signal, timeout/OOM; or nonzero continuous-route warm backend counts. Preserve the first failure, phase, source identities and completed observations. Do not auto-retry, allocate a replacement, change precision/grids or relax the 1.5 progress/off limit. An unavailable allocator instrument or insufficient matched resource evidence leaves the exact G4 item open; only a real maintainer decision may waive a named resource obligation.

## Exit

Root records the G4 result or explicitly retains the current CPU-tested scope without resource/hardware closure. A later `closed` disposition requires that no required gate remains and must name any actual resource waiver and the report entries it covers. There is no third source-discovery round, new code patch or broadened economic-method review queued by this reply.
