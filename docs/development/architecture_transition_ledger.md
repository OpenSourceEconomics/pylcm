---
title: Architecture transition ledger
---

# Architecture transition ledger

The solver, result, and execution architecture is moving in vertical slices. During the
transition, compatibility adapters keep unmigrated solvers and the established
`Model.solve()` interface working. Bootstrap policies and deliberately deferred
integrations keep each slice bounded. Every item below has an explicit replacement or
retirement condition. A plan step is not complete while its corresponding transition
item remains without a current justification.

One invariant is permanent rather than transitional: a result never authenticates its
own values or replay artifacts. Before forward execution, the consuming `Model` owns
immutable descriptors built from its canonical regimes and parameters and, for
data-dependent adaptive axes, a private solve-side record. Those descriptors own the
exact artifact identity and channel, payload type, canonical dtypes, ordered axes and
lengths, state/action roles, categorical code domains, applicability, requiredness, and
consuming replay route. Payloads and repeated public metadata are checked independently
against that authority at every log level. A future descriptor registry may replace
today's private concrete dispatch, but must preserve this direction of authority.

| Transitional implementation                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Exact replacement or retirement condition                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `require_legacy_kernel_result` in the NEGM keeper/adjuster composition, NNBEGM keeper/adjuster composition, and `_TerminalCarryPeriodKernel`, together with the pass-through and refusal tests in `tests/solution/test_kernel_output.py`                                                                                                                                                                                                                                    | Each composite consumes child `KernelOutput` channels directly and publishes its own labelled `KernelOutput`. Delete the adapter, its import/binding certificate entries, and the two adapter-specific tests in the same change; retain tests of the replacement compositions.                                                                                                                                                                                                                                     |
| `PeriodKernel` returning `KernelOutput \| KernelResult`, `normalize_kernel_output`, and the private `KernelResult` transport                                                                                                                                                                                                                                                                                                                                                | The solve loop consumes labelled public output channels directly and every built-in solver publishes `KernelOutput`; no protocol, wrapper, or composite accepts `KernelResult`.                                                                                                                                                                                                                                                                                                                                    |
| Distributed legacy core authority: `PeriodKernel.cores()` enumerates names/functions, `build_lower_args()` builds arguments, optional `CoreProgramAware` repeats program facts, `_TargetValueAccessAware` repeats value-read requirements, `OutputLayoutAware` repeats roles/functions, and period replay reads the legacy methods independently                                                                                                                            | In #433, GridSearch publishes one native program graph as the sole source of core names, raw functions, argument builders, requirements and exact value reads, and output roles; eager, AOT, liveness, layout, and period replay share its resolver. One centralized fail-closed adapter becomes the only reader of legacy declarations. When every built-in kernel publishes a native graph, delete that adapter, the legacy protocols, and their adapter-only tests in the same change.                          |
| The private `_lcm.execution.core_program` types and the current narrow `CoreExecutionRequirements` for action products and exact GridSearch target-value accesses                                                                                                                                                                                                                                                                                                           | Plain EGM, GridSearch, NB-EGM, and an out-of-tree conformance solver have exercised a versioned public contract in `lcm.solver_api`. The public requirements describe all named axes, minimum widths and alignment, legal reordering, core dependencies, input access, reductions, output roles, and replay/retention needs without importing `_lcm`; graph liveness and the planner, not the solver program, select legal donation.                                                                               |
| The private target/source value-address types and the two-operation transfer catalogue, `ALIGNED_LOCAL` and `COPY_TO_SOURCE_LAYOUT`, used by the first GridSearch input-planning slice                                                                                                                                                                                                                                                                                      | A graph-wide input contract covers all built-in solvers and an external conformance route, expresses named-axis/layout conversions, and gives each target-to-source edge one planner-owned transfer. Replace the private address and adapter types and their tests through that seam in the same change. Add another transfer operation before then only for a demonstrated production route with fail-closed metadata, lowering/runtime identity, partial-mesh coverage, and a recorded retirement path.          |
| `PlannedInputLiveness` as eligibility-only remaining-consumer accounting. For planned GridSearch value reads, the transfer gate matches every declared `(source_regime, source_period, core_key)` to the actual compiled `_CoreTriple` and independently resolves its channel/path to the compiled-program argument leaf. Counts commit only after successful dispatch, and dense, legacy, retained-result, and otherwise unplanned consumers remain conservatively pinned. | The graph-wide planner extends that validation to every solve consumer, retention obligation, fold/gated-edge read, alias, and synchronization boundary; distributed tests prove safe physical lifetime transitions. A scheduler then owns release, donation, or offload after successful dispatch. Fold this ledger into that scheduler and delete its tests in the same change. Until then zero is not a memory action and no pin is removed merely because planned counts close.                                |
| Process-local lowering deduplication based on Python callable identity plus the current `CoreProgram.specialization_key` fragment                                                                                                                                                                                                                                                                                                                                           | A durable semantic compilation key covers solver API and kernel versions, core kind and period group, shapes and dtypes, arithmetic profile and topology, resolved layouts and submesh, retention and diagnostic specialization, static model and parameter fingerprints, tile widths, and relevant compiler options. Equivalent separately constructed cores then share a compiled executable without relying on object identity.                                                                                 |
| The initial action-width policy that chooses a bounded power of two below the full product, capped at 64                                                                                                                                                                                                                                                                                                                                                                    | `ExecutionConfig` and the graph-wide planner reject memory-infeasible widths from lowering evidence, benchmark a deterministic feasible frontier on the target hardware, and cache the measured choice by the complete semantic and hardware key. No production test depends on the bootstrap cap or power-of-two rule.                                                                                                                                                                                            |
| The `enable_jit=False` path returning raw legacy `cores()`, plus `CoreProgramAware` opt-out routes that fall back to a dense core                                                                                                                                                                                                                                                                                                                                           | #433 routes native GridSearch eager execution through the same resolved program as AOT and rejects an accidental dense opt-out. Dense GridSearch remains only an independent bounded oracle for migrated routes; deliberately dense production routes carry an explicit disposition. Unmigrated kernels reach eager execution only through the centralized legacy adapter.                                                                                                                                         |
| `_ROLES_FROM_KERNEL`, `OutputLayoutAware`, `UNPLANNED`, `_repair_unplanned_kernel_value`, and the legacy layout-repair tests                                                                                                                                                                                                                                                                                                                                                | In #433, native GridSearch output roles and layouts derive only from its program graph; all remaining legacy role lookup and post-run repair are confined to the centralized adapter. Delete those facilities and their adapter-only tests when the final legacy kernel publishes a native graph; any genuine conversion is then an explicit planned core or edge transfer.                                                                                                                                        |
| GridSearch's parallel `core`, `unwrapped_core`, and `streamed_core` callables beside the planner-selected program                                                                                                                                                                                                                                                                                                                                                           | #433 deletes the parallel fields and every duplicate GridSearch legacy declaration after structural tests prove one native program per mathematical core. The planner exclusively owns compilation, width binding, transfer/liveness resolution, and output lowering; dense GridSearch remains only an independently implemented bounded oracle.                                                                                                                                                                   |
| The first GridSearch `CoreProgram` covers solve-time singleton hard max and EV1 `VALUE`, plus collective `(VALUE, DISSOLUTION_FLAG)`, but publishes no replay or policy artifact                                                                                                                                                                                                                                                                                            | The declared output and retention contract can publish every graph-required channel and any requested replay artifact. Production simulation consumes the declared replay or recomputation route, and value-only execution is a retention specialization rather than a separate solver boundary.                                                                                                                                                                                                                   |
| Dense GridSearch solve fallbacks for co-map routes whose separate same-period or edge-reference channels lack named transfer metadata, and action products of extent at most one, plus dense simulation-policy construction                                                                                                                                                                                                                                                 | Each remaining nontrivial route has its own production `CoreProgram` and route-specific streamed reduction: every co-mapped input channel receives explicit named-axis transfer metadata; simulation retains the requested policy identity or moments. Trivial action products may remain dense only as an explicit no-streaming identity route. Public solve/simulation tests, partial-tile and global-identity oracles, and the candidate certificate cover every migrated route before its fallback is removed. |
| The collective-hard-max and bound log-sum-exp specifications are connected to solve-time full-value reduction, but not yet to simulation replay or policy-moment production                                                                                                                                                                                                                                                                                                 | The corresponding public solve and simulation paths prove that block width cannot change support, feasibility, choice, ties, stakeholder readout, dissolution, or requested policy moments. The independent enumerated oracles remain after this integration item is retired.                                                                                                                                                                                                                                      |
| Separate mapping/tuple `solve()` and labelled `solve_result()` workflows                                                                                                                                                                                                                                                                                                                                                                                                    | In one pre-release change after #433 and before NB-family work, make `solve()` return `SolutionResult` for every supported built-in route and delete `solve_result()` plus all mapping/tuple return variants. Migrate every caller, test, example, notebook, and document in the same change. Add no deprecation alias or compatibility bridge; persistence and durable cross-process identity remain independent later capabilities.                                                                              |
| `return_simulation_policy` and `return_dissolution_flags` on legacy `solve()`                                                                                                                                                                                                                                                                                                                                                                                               | Delete both flags in the same atomic pre-release return-interface change. Replay and collective dissolution data travel under declared model-authoritative artifact keys in `SolutionResult`; no deprecation schedule or flag-selected return union is introduced.                                                                                                                                                                                                                                                 |
| `_resolve_simulation_solution_inputs` adapting labelled results and legacy value/policy/flag mappings                                                                                                                                                                                                                                                                                                                                                                       | The atomic public-return change makes `simulate(solution=...)` consume the canonical `SolutionResult` directly and deletes every legacy mapping/policy/flag branch while preserving model-authoritative preflight in one seam. No versioned compatibility branch is retained before publication.                                                                                                                                                                                                                   |
| `ResultRetention.ALL_PERSISTABLE_ARTIFACTS` currently retains the same replay set as `VALUES_AND_REPLAY`; continuation omissions may be `UNSUPPORTED`, and `SolutionResult` has no persistence or lazy-loading boundary                                                                                                                                                                                                                                                     | Every persistable descriptor supports independently addressable save/load and lazy materialization; the all-persistable mode retains a demonstrably broader set than replay-only, or the redundant mode is removed with a compatibility note. Tests distinguish the two modes through persisted round trips and omission records.                                                                                                                                                                                  |
| `ArtifactStore.project()`                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Replay routes and readers consume addressed artifact stores directly.                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `model_instance_id` and its pickle backfill                                                                                                                                                                                                                                                                                                                                                                                                                                 | A durable semantic model fingerprint is implemented; retain a separate same-instance guard only if it still protects a distinct invariant.                                                                                                                                                                                                                                                                                                                                                                         |
| `collect_simulation_policies`, `simulation_policy_regimes`, `track_artifact_publication`, and `published_simulation_policy_cells`                                                                                                                                                                                                                                                                                                                                           | Role-aware output planning and retention derive publication and omissions from declared `KernelOutput` descriptors.                                                                                                                                                                                                                                                                                                                                                                                                |
| Concrete built-in descriptor construction and replay-validator dispatch in the output bridge and `Model`                                                                                                                                                                                                                                                                                                                                                                    | A dependency-safe descriptor registry owns construction and validation for every installed artifact type without making returned payloads or their public metadata authoritative. The registry must preserve the permanent model-authority invariant above, including independent checks of values and repeated metadata, before this concrete dispatch is removed.                                                                                                                                                |
| `GeneratedReplayAuthority` and `Model._solution_authorities`, the private solve sidecar and per-parameter cache of exact data-dependent adaptive replay-axis coordinates; storage grows only with distinct adaptive-route parameter fingerprints and relies on the deterministic mesh for equal model/parameter inputs                                                                                                                                                      | Stable persistence provides a model-verifiable semantic descriptor for dynamic solver output contracts, or the model can independently rederive those exact axes without consulting returned artifacts. Then delete the process-local sidecar/cache and its pickle transport while retaining fail-closed preflight.                                                                                                                                                                                                |
| The private `SolverDiagnostics` result channel and its collection flags                                                                                                                                                                                                                                                                                                                                                                                                     | Diagnostics are labelled artifacts whose construction and retention are governed only by `log_level`.                                                                                                                                                                                                                                                                                                                                                                                                              |
| Hard-coded EGM continuation omissions marked `UNSUPPORTED`                                                                                                                                                                                                                                                                                                                                                                                                                  | Continuations can be retained and persisted independently through their artifact descriptor.                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `ContinuationPayload = EGMCarry` as the concrete rolling-continuation alias                                                                                                                                                                                                                                                                                                                                                                                                 | A keyed/versioned continuation operation or reader protocol supports every retained representation and every producer/consumer has migrated. Do not broaden the alias to `object`; consumers must request declared operations rather than concrete fields.                                                                                                                                                                                                                                                         |
| Legacy value-only `save_solution` beside an in-memory-only `SolutionResult`                                                                                                                                                                                                                                                                                                                                                                                                 | A versioned labelled-result persistence format has a durable model fingerprint, independently addressable artifact schemas, atomic save/load, lazy materialization, omission semantics, and cross-process corruption/version tests. Preserve the legacy loader and provide migration until its release-policy gate closes.                                                                                                                                                                                         |
| Candidate-certificate source seals anchored to the current migration seams                                                                                                                                                                                                                                                                                                                                                                                                  | Any callable move, bridge deletion, or source-route change deliberately regenerates and reviews the source inventory, then passes both precision profiles, every synchronized mutation, and every source-set perturbation. Never refresh a seal merely to make a changed route green.                                                                                                                                                                                                                              |
| Documentation that marks the labelled result and out-of-tree solver surfaces experimental                                                                                                                                                                                                                                                                                                                                                                                   | Narrow or remove the warning only after durable identity and persistence, the external conformance suite, public compatibility/versioning policy, and an explicit maintainer stability decision exist. Compile-only experiment wording remains separate.                                                                                                                                                                                                                                                           |
| The compile-only fused NB-EGM replay and analyzer path                                                                                                                                                                                                                                                                                                                                                                                                                      | The tile-local production core is live, publishes solve-time outputs directly in planned layouts, and a bounded split implementation remains only as its independent oracle.                                                                                                                                                                                                                                                                                                                                       |
| Hardware semantics in grid `batch_size` and `distributed` fields                                                                                                                                                                                                                                                                                                                                                                                                            | `ExecutionConfig` and resolved planner layouts own the corresponding placement and tile choices; grids retain only economic support, ordering, interpolation, and quadrature semantics.                                                                                                                                                                                                                                                                                                                            |

When a new adapter, bootstrap policy, or deferred integration is introduced, add it here
in the same change. Remove its row only after the retirement condition is covered by a
test through the replacement seam.
