"""The solve phase: the backward-induction loop and the solvers it drives.

`backward_induction.py` is the loop, `contract.py` the seam every solver meets
it through, `grid_search.py` and the EGM-family modules the shipped solvers.
The remaining modules plan, admit and attribute the work the loop dispatches.
"""
