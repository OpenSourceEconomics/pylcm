"""Implementation of the public `lcm.solver_api` surface.

Every name a solver author or the engine reaches lives on
`lcm.solver_api`; these modules hold the definitions without importing
anything engine-private.
"""
